These files are archived for reference only.

They are not part of the Monday local demo path.

Use the repository-root commands instead:

`docker compose up -d`

`docker compose down`

`docker compose down -v`
